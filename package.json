{
  "name": "exchange-ts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "EXCHANGE_ADMIN_ENABLED=1 concurrently -n engine,ledger,gateway,marketdata,ui -c yellow,green,cyan,magenta,white \"pnpm --filter engine dev\" \"pnpm --filter ledger dev\" \"pnpm --filter gateway dev\" \"pnpm --filter marketdata dev\" \"pnpm --filter ui dev\"",
    "dev:go": "EXCHANGE_ADMIN_ENABLED=1 concurrently -n engine,ledger,gateway,marketdata,ui -c yellow,green,cyan,magenta,white \"pnpm dev:go:engine\" \"pnpm dev:go:ledger\" \"pnpm --filter gateway dev\" \"pnpm --filter marketdata dev\" \"pnpm --filter ui dev\"",
    "dev:go:engine": "cd packages-go/engine && go run .",
    "dev:go:ledger": "cd packages-go/ledger && go run .",
    "build:go": "pnpm build:go:engine && pnpm build:go:ledger",
    "build:go:engine": "cd packages-go/engine && go build -o ../../dist/ .",
    "build:go:ledger": "cd packages-go/ledger && go build -o ../../dist/ .",
    "simulation": "pnpm --filter @exchange/simulation start",
    "reset": "pnpm --filter @exchange/simulation reset",
    "redis:up": "docker compose up -d redis",
    "redis:down": "docker compose down",
    "redis:reset": "docker compose down -v && docker compose up -d redis"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
